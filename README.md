backend

1. cd into backend
2. first setup create virtual environment "python3 -m venv venv"
3. activate virtual environment "source venv/bin/activate"
4. pip install "fastapi[standard]"
5. run with "fastapi dev main.py"
